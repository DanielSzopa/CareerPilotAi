@model CareerPilotAi.ViewModels.Authentication.ResetPasswordViewModel

@{
    ViewData["Title"] = "Reset Password";
}

<link rel="stylesheet" href="~/css/auth.css" />

<div>
    <div class="d-flex justify-content-center align-items-center">
        <div class="auth-card bg-white rounded py-5 px-4">
            <div class="text-center mb-4">
                <h4 class="auth-title">Reset Your Password</h4>
                <p class="text-muted">Enter your new password below</p>
            </div>

            <form asp-action="ResetPassword" method="post" data-test-id="reset-password-form">
                <div asp-validation-summary="All" class="text-danger mb-3" data-test-id="validation-summary"></div>
                
                <input asp-for="UserId" type="hidden" />
                <input asp-for="Code" type="hidden" />
                
                <div class="form-group mb-3">
                    <div class="password-field">
                        <input asp-for="Password" class="form-control auth-input" placeholder="New password" data-test-id="password-input" />
                        <button type="button" class="password-toggle" data-test-id="password-toggle-button">
                            <i class="fas fa-eye" id="passwordToggleIcon"></i>
                        </button>
                    </div>
                    <span asp-validation-for="Password" class="text-danger" data-test-id="password-validation"></span>
                </div>

                <div class="form-group mb-3">
                    <div class="password-field">
                        <input asp-for="ConfirmPassword" class="form-control auth-input" placeholder="Confirm new password" data-test-id="confirm-password-input" />
                        <button type="button" class="password-toggle" data-test-id="confirm-password-toggle-button">
                            <i class="fas fa-eye" id="confirmPasswordToggleIcon"></i>
                        </button>
                    </div>
                    <span asp-validation-for="ConfirmPassword" class="text-danger" data-test-id="confirm-password-validation"></span>
                </div>
                
                <div class="form-group mb-3">
                    <button type="submit" class="btn btn-primary action-button w-100" data-test-id="reset-password-button">Reset Password</button>
                </div>
                
                <div class="text-center">
                    <p class="text-muted small m-0">
                        Remember your password? <a asp-action="Login" class="link fw-medium" data-test-id="back-to-login-link">Back to Login</a>
                    </p>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
    @await Html.PartialAsync("../Auth/Shared/_AuthScripts.cshtml")
}
